fastapi==0.104.1
uvicorn==0.24.0.post1
sqlalchemy==2.0.23
asyncpg==0.29.0
pydantic==2.5.2
pydantic[email]==2.5.2
face_recognition==1.3.0
opencv-python-headless==4.8.1.78
PyJWT==2.8.0
bcrypt==5.0.0
python-dotenv==1.0.0
# Stage 2 — AI Recipe Agent (Semantic Search)
# groq 0.11.0 + httpx 0.27.2 must be pinned together.
# groq's internal SyncHttpxClientWrapper passes 'proxies' to httpx.Client,
# which was removed in httpx 0.28.0 — pinning 0.27.2 keeps them compatible.
groq==0.11.0
httpx==0.27.2
# pgvector: Python client for PostgreSQL vector similarity search
pgvector==0.3.6
# sentence-transformers: all-MiniLM-L6-v2 for 384-dim semantic embeddings
sentence-transformers==3.3.1
